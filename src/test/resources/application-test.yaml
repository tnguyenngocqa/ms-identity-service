spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MYSQL # In-memory database URL
    driver-class-name: org.h2.Driver
    username: sa
    password: sa
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: none
